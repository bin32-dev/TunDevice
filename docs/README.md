# Documentation Output

Run the following command to generate HTML API documentation:

```bash
cmake --build build --target doc
```

When Doxygen is available, output will be generated under `docs/html/`.
